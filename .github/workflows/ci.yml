name: Simple Example

on: [push]

jobs:
  CI_Steps:

    runs-on: ubuntu-latest
    steps:
      - name: Code checkout
        uses: actions/checkout@v4

      - name: Code Build
        run: echo "Ok"

      - name: Code Test
        run: echo "Ok"

      - name: Code Review
        run: echo "Ok"

      - name: Relea se software
        run: echo "Ok"



